STAGEDIR=	/usr/lib
DIR=		lua
SHLIBDIR= 	${STAGEDIR}${DIR}
LIBDIR= 	${STAGEDIR}${DIR}
SUBDIR=		lua luadns luaexpat luaposix luasocket luasysctl luasyslog

PREINSTALLDIRS+=${STAGEDIR}${DIR}

.include <bsd.subdir.mk>
